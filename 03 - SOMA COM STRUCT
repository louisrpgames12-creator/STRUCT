#include <stdio.h>
typedef struct {
    float x;
    float y;    
    float z;
} rs;

int main() {
    int i;
    rs vetor[2];
    rs vetorsoma = {0,0,0};
    for(i = 0; i < 2; i++) {
        printf("Me informe o x: ");
        scanf(" %f", &(vetor + i)->x);
        vetorsoma.x += (vetor + i)->x;
        printf("Me informe o y: ");
        scanf(" %f", &(vetor + i)->y);
        vetorsoma.y += (vetor + i) -> y;
        printf("Me informe o z: ");
        scanf(" %f", &(vetor + i)->z);
        vetorsoma.z += (vetor + i) -> z;
    }
    
        printf("R3 (%.1f,%.1f,%1.f)", vetorsoma.x, vetorsoma.y, vetorsoma.z);
    
    return 0;
}
